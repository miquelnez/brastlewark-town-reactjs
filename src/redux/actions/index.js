import { brastlewarkActions } from './brastlewarkActions';
import { paginationActions } from './paginationActions';

const allActions = {
    brastlewarkActions,
    paginationActions
};

export default allActions;